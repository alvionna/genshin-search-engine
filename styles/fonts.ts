import { Inter, Imbue } from 'next/font/google';
 
export const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
});
 
export const imbue = Imbue({
  subsets: ['latin'],
  display: 'swap',
});